---
import { getTaxonomyTree } from '../lib/data';

const taxonomy = getTaxonomyTree();
const concepts = taxonomy.level_2_and_3.concepts;
const currentPath = Astro.url.pathname;

const conceptThemes: Record<string, string> = {
  "c1": "var(--color-security)",
  "c2": "var(--color-network)",
  "c3": "var(--color-compute)",
  "c4": "var(--color-storage)",
  "c5": "var(--color-database)",
  "c6": "var(--color-decouple)"
};
---

<aside class="sidebar-wrapper" id="sidebar">
  <div class="sidebar-inner">
    <div class="sidebar-header">
      <span class="sb-title">STUDY BLUEPRINT</span>
      <span class="sb-badge">SAA-C03</span>
    </div>

    <nav class="sidebar-nav">
      <!-- CORE NAVIGATION -->
      <div class="sb-section">
        <a href="/practice" class={`sb-nav-link ${currentPath === '/practice' ? 'active' : ''}`}>
          <span class="sb-mono">[ARENA]</span>
          <span>Practice Arena</span>
        </a>
        <a href="/services" class={`sb-nav-link ${currentPath === '/services' ? 'active' : ''}`}>
          <span class="sb-mono">[CATALOG]</span>
          <span>Services Directory</span>
        </a>
        <a href="/concepts" class={`sb-nav-link ${currentPath === '/concepts' ? 'active' : ''}`}>
          <span class="sb-mono">[TREE]</span>
          <span>Taxonomy Tree</span>
        </a>
      </div>

      <!-- DOMAINS & CLUSTERS -->
      <div class="sb-tree-header">// DECISION DOMAINS</div>

      {concepts.map((concept: any) => {
        const themeColor = conceptThemes[concept.id] || "var(--color-network)";
        return (
          <details class="sb-concept-group" open>
            <summary class="sb-concept-summary" style={`border-left: 2px solid ${themeColor};`}>
              <span class="sb-concept-name">{concept.name}</span>
              <span class="sb-arrow">▾</span>
            </summary>

            <div class="sb-clusters-list">
              {concept.clusters.map((cluster: any) => {
                const topicUrl = `/topics/${cluster.id}`;
                const isTopicActive = currentPath === topicUrl;
                return (
                  <div class="sb-cluster-item">
                    <a href={topicUrl} class={`sb-cluster-link ${isTopicActive ? 'active' : ''}`}>
                      <span class="sb-square" style={`background: ${themeColor};`}></span>
                      <span>{cluster.name}</span>
                    </a>

                    <div class="sb-services-sublist">
                      {cluster.services.map((svcName: string) => {
                        const svcId = svcName.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '');
                        const svcUrl = `/services/${svcId}`;
                        const isSvcActive = currentPath === svcUrl;
                        return (
                          <a href={svcUrl} class={`sb-svc-link ${isSvcActive ? 'active' : ''}`}>
                            {svcName}
                          </a>
                        );
                      })}
                    </div>
                  </div>
                );
              })}
            </div>
          </details>
        );
      })}
    </nav>
  </div>
</aside>

<style>
  .sidebar-wrapper {
    width: 260px;
    flex-shrink: 0;
    position: sticky;
    top: 53px;
    height: calc(100vh - 53px);
    border-right: 1px solid var(--border-subtle);
    background: #090a0d;
    overflow-y: auto;
  }

  @media (max-width: 900px) {
    .sidebar-wrapper {
      display: none;
    }
  }

  .sidebar-inner {
    padding: 1rem 0.75rem 3rem;
  }

  .sidebar-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: 0.75rem;
    margin-bottom: 1rem;
    border-bottom: 1px solid var(--border-subtle);
  }

  .sb-title {
    font-size: 0.65rem;
    font-weight: 900;
    color: var(--text-muted);
    font-family: var(--font-mono);
    letter-spacing: 0.08em;
  }

  .sb-badge {
    font-size: 0.6rem;
    font-weight: 900;
    background: rgba(255, 42, 133, 0.1);
    color: var(--color-security);
    border: 1px solid var(--color-security);
    padding: 0.1rem 0.35rem;
    font-family: var(--font-mono);
  }

  .sb-section {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    margin-bottom: 1.25rem;
  }

  .sb-nav-link {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.45rem 0.5rem;
    color: var(--text-secondary);
    text-decoration: none;
    font-size: 0.82rem;
    font-weight: 700;
    border: 1px solid transparent;
  }

  .sb-nav-link.active {
    background: rgba(255, 255, 255, 0.04);
    color: var(--text-primary);
    border-color: var(--border-subtle);
  }

  .sb-mono {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    color: var(--text-muted);
  }

  .sb-tree-header {
    font-size: 0.6rem;
    font-weight: 900;
    color: var(--text-muted);
    font-family: var(--font-mono);
    margin-bottom: 0.5rem;
    padding-left: 0.25rem;
  }

  .sb-concept-group {
    margin-bottom: 0.5rem;
  }

  .sb-concept-summary {
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.4rem 0.5rem;
    cursor: pointer;
    font-size: 0.8rem;
    font-weight: 800;
    color: var(--text-primary);
    background: rgba(255, 255, 255, 0.015);
    margin-bottom: 0.25rem;
    border-bottom: 1px solid var(--border-subtle);
  }

  .sb-concept-summary::-webkit-details-marker {
    display: none;
  }

  .sb-concept-name {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .sb-arrow {
    font-size: 0.7rem;
    color: var(--text-muted);
  }

  .sb-clusters-list {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    padding-left: 0.5rem;
    margin-top: 0.25rem;
  }

  .sb-cluster-item {
    display: flex;
    flex-direction: column;
  }

  .sb-cluster-link {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.3rem 0.4rem;
    font-size: 0.78rem;
    font-weight: 700;
    color: var(--text-secondary);
    text-decoration: none;
  }

  .sb-cluster-link.active {
    color: var(--text-primary);
    font-weight: 900;
    background: rgba(255, 255, 255, 0.04);
  }

  .sb-square {
    width: 5px;
    height: 5px;
    flex-shrink: 0;
  }

  .sb-services-sublist {
    display: flex;
    flex-wrap: wrap;
    gap: 0.25rem;
    padding-left: 0.85rem;
    margin-top: 0.2rem;
    margin-bottom: 0.4rem;
  }

  .sb-svc-link {
    font-size: 0.68rem;
    font-family: var(--font-mono);
    color: var(--text-muted);
    text-decoration: none;
    padding: 0.1rem 0.35rem;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid var(--border-subtle);
  }

  .sb-svc-link.active {
    color: var(--color-network);
    border-color: var(--color-network);
    font-weight: 800;
  }
</style>
