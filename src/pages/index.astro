---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getPracticeQuestions } from '../lib/data';

const questions = getPracticeQuestions();
---

<BaseLayout title="SAA-C03 | Practice Arena">
  <div class="simulator-container glass-card">
    {questions.map((q, idx) => (
      <div class="question-block" id={`q-${q.id}`} style={idx === 0 ? "" : "display: none;"}>
        <div class="question-meta">
          <span class="question-number">QUESTION {q.id} OF {questions.length}</span>
        </div>

        <h2 class="question-text">{q.question}</h2>

        <div class="options-group">
          {q.options.map((opt) => (
            <button class="option-btn" data-answer={q.answer} data-opt={opt.charAt(0)}>
              {opt}
            </button>
          ))}
        </div>

        <div class="answer-section" style="display: none;">
          <div class="answer-badge">ANSWER: {q.answer}</div>
          <div class="explanation-box">
            <ul>
              {q.explanation.map((exp) => (
                <li>{exp}</li>
              ))}
            </ul>
          </div>
        </div>

        <div class="controls-bar">
          <button class="btn-ctrl btn-prev" disabled={idx === 0}>← Prev</button>
          <button class="btn-ctrl btn-reveal">Show Answer</button>
          <button class="btn-ctrl btn-next" disabled={idx === questions.length - 1}>Next →</button>
        </div>
      </div>
    ))}
  </div>
</BaseLayout>

<script>
  let currentIndex = 0;
  const totalQuestions = document.querySelectorAll('.question-block').length;

  function updateQuestion(newIndex: number) {
    const currentBlock = document.querySelectorAll('.question-block')[currentIndex] as HTMLElement;
    const nextBlock = document.querySelectorAll('.question-block')[newIndex] as HTMLElement;
    if (currentBlock && nextBlock) {
      currentBlock.style.display = 'none';
      nextBlock.style.display = 'block';
      currentIndex = newIndex;
    }
  }

  document.querySelectorAll('.btn-next').forEach(btn => {
    btn.addEventListener('click', () => {
      if (currentIndex < totalQuestions - 1) updateQuestion(currentIndex + 1);
    });
  });

  document.querySelectorAll('.btn-prev').forEach(btn => {
    btn.addEventListener('click', () => {
      if (currentIndex > 0) updateQuestion(currentIndex - 1);
    });
  });

  document.querySelectorAll('.btn-reveal').forEach(btn => {
    btn.addEventListener('click', (e) => {
      const parent = (e.target as HTMLElement).closest('.question-block');
      const answerSection = parent?.querySelector('.answer-section') as HTMLElement;
      if (answerSection) {
        answerSection.style.display = answerSection.style.display === 'none' ? 'block' : 'none';
      }
    });
  });

  document.querySelectorAll('.option-btn').forEach(btn => {
    btn.addEventListener('click', (e) => {
      const target = e.currentTarget as HTMLElement;
      const parent = target.closest('.question-block');
      const correctAns = target.getAttribute('data-answer');
      const chosen = target.getAttribute('data-opt');

      parent?.querySelectorAll('.option-btn').forEach(b => {
        b.classList.remove('selected', 'correct', 'wrong');
      });

      if (chosen === correctAns) {
        target.classList.add('correct');
      } else {
        target.classList.add('wrong');
      }
    });
  });
</script>

<style>
  .simulator-container {
    padding: 1.75rem;
    max-width: 800px;
    margin: 0 auto;
  }
  .question-meta {
    margin-bottom: 1rem;
  }
  .question-number {
    font-weight: 800;
    color: var(--text-muted);
    font-size: 0.75rem;
    letter-spacing: 0.05em;
  }
  .question-text {
    font-size: 1.1rem;
    line-height: 1.55;
    margin-bottom: 1.5rem;
    font-weight: 600;
  }
  .options-group {
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
    margin-bottom: 1.5rem;
  }
  .option-btn {
    background: #06060a;
    border: 1px solid var(--border-subtle);
    color: var(--text-primary);
    padding: 0.75rem 1rem;
    border-radius: 6px;
    text-align: left;
    font-size: 0.95rem;
    cursor: pointer;
    transition: all 0.15s;
  }
  .option-btn:hover {
    background: #12121a;
    border-color: #2d2b55;
  }
  .option-btn.correct {
    background: rgba(0, 255, 170, 0.1);
    border-color: var(--unicorn-emerald);
    color: var(--unicorn-emerald);
  }
  .option-btn.wrong {
    background: rgba(255, 42, 133, 0.1);
    border-color: var(--unicorn-pink);
    color: var(--unicorn-pink);
  }
  .answer-section {
    margin-bottom: 1.5rem;
    padding: 1rem;
    background: #0a0a10;
    border-radius: 6px;
    border-left: 3px solid var(--unicorn-pink);
  }
  .answer-badge {
    font-weight: 800;
    font-size: 0.85rem;
    color: var(--unicorn-emerald);
    margin-bottom: 0.5rem;
    letter-spacing: 0.05em;
  }
  .explanation-box ul {
    list-style-type: disc;
    padding-left: 1rem;
    color: var(--text-secondary);
    font-size: 0.85rem;
  }
  .explanation-box li {
    margin-bottom: 0.3rem;
  }
  .controls-bar {
    display: flex;
    justify-content: space-between;
    gap: 0.75rem;
    border-top: 1px solid var(--border-subtle);
    padding-top: 1rem;
  }
  .btn-ctrl {
    padding: 0.45rem 0.9rem;
    border-radius: 6px;
    font-weight: 700;
    font-size: 0.8rem;
    background: #0a0a0f;
    border: 1px solid var(--border-subtle);
    color: var(--text-primary);
    cursor: pointer;
  }
  .btn-ctrl:disabled {
    opacity: 0.3;
    cursor: not-allowed;
  }
  .btn-ctrl:not(:disabled):hover {
    border-color: var(--unicorn-pink);
    color: var(--unicorn-pink);
  }
</style>
